const header=document.querySelector('[data-site-header]');const menuToggle=document.querySelector('[data-menu-toggle]');const navigation=document.querySelector('[data-site-nav]');if(header&&menuToggle&&navigation){const mobileQuery=window.matchMedia('(max-width: 760px)');const setMenuState=open=>{header.classList.toggle('is-menu-open',open);menuToggle.setAttribute('aria-expanded',String(open));document.body.classList.toggle('has-menu-open',open)};const updateHeaderState=()=>header.classList.toggle('is-scrolled',window.scrollY>12);menuToggle.addEventListener('click',()=>setMenuState(menuToggle.getAttribute('aria-expanded')!=='true'));navigation.addEventListener('click',event=>{if(mobileQuery.matches&&event.target.closest('a'))setMenuState(false)});document.addEventListener('keydown',event=>{if(event.key==='Escape'&&menuToggle.getAttribute('aria-expanded')==='true'){setMenuState(false);menuToggle.focus()}});mobileQuery.addEventListener('change',event=>{if(!event.matches)setMenuState(false)});window.addEventListener('scroll',updateHeaderState,{passive:true});updateHeaderState()}
